# 智谱AI配置 (可选,用于AI功能)
# 获取方式: https://open.bigmodel.cn/
GLM_API_KEY=your_glm_api_key_here
GLM_MODEL=glm-4-flash

# 数据库配置（如果需要覆盖docker-compose.yml中的配置）
# DATABASE_URL=postgresql://user:password@localhost:5432/campuswap
